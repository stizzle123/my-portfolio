export interface IProps {
  setTitle: any;
  setTimeline: any;
  type?: string;
}

export interface ItemProps {
  date?: string;
  company?: string;
  jobTitle?: string;
  description?: string;
}

export interface ThemeType {
  type?: string;
}
